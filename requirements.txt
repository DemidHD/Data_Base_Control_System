# Веб-фреймворк и ASGI-сервер
fastapi==0.136.0
uvicorn==0.44.0

# ORM и валидация
tortoise-orm==1.1.7
pydantic==2.13.3

# Шаблоны (templates/index.html)
jinja2==3.1.2

# Конфигурация через .env
python-dotenv==1.2.2

# Драйверы БД
aiosqlite==0.22.1   # SQLite — используется по умолчанию
asyncpg==0.31.0     # PostgreSQL — если задан DATABASE_URL=postgres://...
